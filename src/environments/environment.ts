export const environment = {
  /**
   * '/api' relies on the dev-server proxy (proxy.conf.json) or a same-origin
   * reverse proxy in production — the backend has no CORS configured.
   */
  apiUrl: '/api',
};
